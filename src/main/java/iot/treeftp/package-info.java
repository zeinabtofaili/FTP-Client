/**
 * This package includes classes for managing connections with FTP servers as
 * well as performing some actions such as listing the contents of directories
 * in the FTP server in a tree format.
 * <p>
 * It contains 2 classes, the driver class "Main" that contains the main method
 * and runs the application, and the "FTPClient" class which contains methods
 * useful for connecting to servers, logging in, listing directories, and so on.
 *
 */
package iot.treeftp;
